// @flow
import { numToArr } from '../../util.js'
export { numToArr }
export const SELECT_COLOR = 'rgb(76, 97, 135)'
export const scrape = (arr) => arr.filter((content) => content)
export const sum = (arr) => arr.reduce((p, c) => p + c)